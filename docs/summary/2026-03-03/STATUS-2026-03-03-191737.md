# Archery V3 - Project Status

**Date:** March 3, 2026  
**Snapshot Time:** 19:17:37  
**Last Commit:** `e8cfe38` (`fix: resolve OAuth signup loop caused by stale auth trigger`)

---

## ✅ What Is Working Now

### Auth
- ✅ Google OAuth login works on production (`archery-v3-supabase.vercel.app`)
- ✅ Callback origin handling is stable (no redirect host mismatch)
- ✅ Supabase Auth URL configuration updated for localhost + production callbacks

### 1-vs-1 Match Core Flow
- ✅ Create invitation
- ✅ Accept invitation
- ✅ Decline invitation reflects correctly for challenger dashboard state
- ✅ Save match session and submit scores
- ✅ Winner calculation and result card display

---

## 🔧 Bugs Fixed In This Session

### 1) OAuth Login Loop / "Database error saving new user"
**Root cause:** Stale `auth.users` trigger function `resolve_match_player_b()` referenced removed columns from old schema.

**Fix applied:**
- Replaced trigger function logic to update `match_invitations.invitee_user_id` safely.
- Added migration: `supabase/migrations/20260304010000_fix_resolve_match_player_b_trigger.sql`.

### 2) Stuck "Match in Progress" after both players submitted
**Root cause:** Completion checks relied on cross-user `sessions` reads, but RLS only exposes current user's sessions.

**Fix applied:**
- Updated app-side completion checks to use match totals (`matches.challenger_total/opponent_total`) instead of cross-user session visibility.
- Deployed to production.

### 3) Existing stale active matches in production
**Root cause:** Historical rows remained `active` even with both scores present.

**Fix applied:**
- Ran SQL cleanup to auto-complete stale active matches where both totals are present.
- Re-ran winner calculation.
- Current status counts: `active=0`, `completed=4`, `cancelled=10`.

---

## 🧪 Validation Performed

- Verified OAuth sign-in succeeds after trigger fix.
- Queried live DB to confirm no stale `active` matches remain.
- Deployed latest production build after match completion logic update.

---

## 📌 Next Planned Work

- Add completed-match visibility into dashboard calendar as enriched session entries (no synthetic events).
- Rules confirmed:
  - show only completed matches,
  - one consolidated match item per user,
  - subtle badge,
  - distinct calendar marker color,
  - perspective score summary.
